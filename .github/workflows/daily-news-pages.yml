name: Daily Janet News

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Daily generator disabled
        run: |
          echo "janet-public-site daily news generator is disabled."
          echo "Use /Volumes/Janet/codex-briefing-system/runs/YYYY-MM-DD/content.json as the briefing source."
