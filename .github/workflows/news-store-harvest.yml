name: News Store Harvest

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: News store harvest disabled
        run: |
          echo "janet-public-site news-store harvest is disabled."
          echo "Codex briefing system is now the only daily briefing data source."
