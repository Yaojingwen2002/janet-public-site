# Optional local paths only. No model/search API keys belong here.
# PUBLIC_SITE_DIR defaults to repo root (janet-public-site) when unset.
# Optional. Defaults to ./templates/template.html when unset.
# TEMPLATE_PATH=/Volumes/Janet/janet-public-site/codex-briefing-system/templates/template.html

# Future optional CLI mode only. Leave unset for current Codex App workflow.
CODEX_BIN=
CODEX_MODEL=gpt-5.5
